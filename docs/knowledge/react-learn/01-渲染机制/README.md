# 01 渲染机制

**状态：★ 已掌握**

## 知识点

- [ ] JSX 语法规则（`{}` 表达式、`className`、`style={}`）
- [ ] 条件渲染（`&&`、三元、`if/else`）
- [ ] 列表渲染（`map` + `key`）
- [ ] 组件定义（函数组件）
- [ ] Props 传递（单向数据流）
- [ ] 组件组合（`children`）
- [ ] Fragment（`<>...</>`）

## 项目中的例子

- `pages/dashboard/index.tsx` — 组件组合
- `pages/trades/index.tsx` — 条件渲染 + 列表渲染
- `components/common/FormInput.tsx` — Props 接口

## 常见面试题

- React 的渲染流程是怎样的？
- key 的作用是什么？
- 为什么 Props 不能直接修改？
