# 07 UI 库

**状态：★ 已掌握**

## 知识点

- [ ] Tailwind CSS v4 — 原子化 CSS，CSS-based 配置
- [ ] daisyUI v5 — Tailwind 的组件库（btn、card、table、badge 等）
- [ ] Ant Design v6 — `ConfigProvider` 主题注入
- [ ] 主题切换（light / dark）
- [ ] 响应式布局（`md:`、`lg:` 断点）
- [ ] 自定义 daisyUI 主题变量

## 项目中的例子

- `index.css` — `@import "tailwindcss"` + `@plugin "daisyui"` + 自定义主题
- `components/layout/AppLayout.tsx` — daisyUI drawer 导航
- `components/common/Paginator.tsx` — daisyUI join + btn 分页

## 常见面试题

- 原子化 CSS 和传统 CSS 的优劣？
- daisyUI 和 shadcn/ui 的本质区别？
- Tailwind v4 和 v3 的配置区别？
